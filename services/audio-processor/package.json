{
  "name": "@voice/audio-processor",
  "private": true,
  "version": "0.1.0",
  "description": "Job contract types for the Python audio processing service",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@voice/shared-types": "workspace:*"
  }
}
